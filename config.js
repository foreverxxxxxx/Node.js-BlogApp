const config={
    db :{
        
            host: "localhost",
            user:"root",
            password :"your password",
            database : "blogapp"
        
    }
 }
module.exports=config;// We are opening it to the outside